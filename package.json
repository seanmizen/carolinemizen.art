{
  "name": "carolinemizen.art",
  "private": true,
  "workspaces": [
    "src/frontend"
  ],
  "scripts": {
    "build": "tsc",
    "start": "cd src/frontend && bun dev",
    "start-old": "tsc --watch",
    "format": "prettier --write \"src/**/*.ts\"",
    "format-check": "prettier --check \"src/**/*.ts\""
  },
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "prettier": "^3.2.4"
  }
}
